{
  "_version": "1.0.0",
  "_comment": "For GUI-specific configs, see settings.*.example.json files",
  
  "paths": {
    "pai_root": "${PAI_ROOT}",
    "skills_dir": "${PAI_ROOT}\\skills",
    "agents_dir": "${PAI_ROOT}\\agents",
    "commands_dir": "${PAI_ROOT}\\commands",
    "history_dir": "${PAI_ROOT}\\history",
    "tools_dir": "${PAI_ROOT}\\tools"
  },
  
  "user": {
    "name": "Your Name",
    "timezone": "Europe/Zurich"
  },
  
  "defaults": {
    "assistant_name": "Clippy",
    "agent": "assistant",
    "skills_always_load": ["CORE"]
  }
}